# Project: PrayasUP — UPPSC Exam Prep Platform (working name)

## What this is
A bilingual (Hindi/English equal-first) web platform for UPPSC (UP PCS) aspirants.
Flagship feature: AI Answer Writing Evaluation for Mains descriptive answers
(typed or handwritten-photo upload), scored against a UPPSC-style rubric with
streaming feedback. Also: PYQ-based MCQ practice, syllabus-mapped learning,
AI-summarized current affairs (UP-specific focus), FSRS spaced repetition,
analytics, RAG doubt-solving chatbot.

## Architecture (FIXED — never deviate)
- Monorepo: /apps/web (Vite + React 19 SPA, TypeScript, React Router v7),
  /apps/api (Express, TypeScript), /packages/shared (types, zod schemas).
- NO Next.js anywhere. The web app is a pure SPA; the ONLY backend is the
  Express API.
- DB: Supabase cloud Postgres + pgvector. SAME project for local dev and
  production. All schema changes via SQL migration files in
  /supabase/migrations, applied with supabase CLI (db push).
- UI: shadcn/ui + Tailwind v4. Server state: TanStack Query. Client state:
  Zustand (minimal). i18n: react-i18next; all app URLs are /:locale/* with en
  as default.
- Fonts: Inter (latin) + Noto Sans Devanagari via Fontsource. Hindi typography
  is first-class, never a fallback.
- LLM (app runtime): Anthropic API — claude-sonnet-5 for answer evaluation +
  doubt chat; claude-haiku-4-5 for high-volume tasks (summaries, MCQ
  explanations, translation drafts). Model ids live in ONE constants module,
  never inline.
- Embeddings: OpenAI text-embedding-3-small (1536-dim, matches vector(1536)),
  behind src/lib/embeddings.ts so the provider is swappable.
- All AI responses stream via SSE from Express under /api/v1/stream/*. The web
  app consumes SSE ONLY through src/lib/sse.ts built on
  @microsoft/fetch-event-source (POST + Authorization headers — required once
  auth lands in Session 15).
- NO mock data anywhere. Every screen reads real rows from Supabase.

## Dev conventions
- pnpm workspaces. `pnpm dev` runs web (:3000) + api (:4000) concurrently.
- Env: /apps/web/.env.local uses VITE_* and holds browser-safe values ONLY
  (Supabase URL, anon key, API URL). /apps/api/.env holds all secrets (service
  role, ANTHROPIC_API_KEY, OPENAI_API_KEY). .env.example files always current.
- Until auth exists (Session 15): all API calls act as the seeded dev user
  (DEV_USER_ID in api env; never hardcode).
- Bilingual content: JSONB {"hi","en"} columns named *_i18n. Publish gate =
  both languages present.
- API: Express routes under /api/v1/*, zod-validated inputs, {data,error}
  envelope. SSE endpoints under /api/v1/stream/*.
- Routing: app pages under /:locale/*; route modules lazy; anything shareable
  (filters, tabs) lives in URL/search params.
- Content acquisition is automated: /content-sources.yaml + `pnpm
  content:fetch` download real UPPSC files into /content-raw (binaries
  gitignored; manifest committed). NEVER ask the user to manually download
  something the script can fetch; on fetch failure, print the exact URL for a
  one-click manual grab.
- Commit after every working feature. Never commit secrets. Mobile-first:
  verify every screen at 390px.

## Definition of done for any UI session
Runs locally with `pnpm dev`, renders REAL Supabase data, works at 390px and 1440px, both locales render (language toggle), no console errors.

## Session log
- Session 0 (2026-07-05): repo initialized, CLAUDE.md created, empty workspace skeleton committed. Pushed to private GitHub repo github.com/ayushhh-singh/studyApp.
- Pre-Session-1 (2026-07-05): apps/api/.env and apps/web/.env.local created (gitignored). ANTHROPIC_API_KEY and OPENAI_API_KEY reused from the same account's nyay-sahayak project. SUPABASE_URL/keys still need a NEW cloud Supabase project — not reused, since nyay-sahayak's is a local-only instance with an unrelated schema.
- Session 1 (2026-07-05): scaffolded the full monorepo.
  - apps/web: Next.js 15 (App Router, TS, Tailwind v4, src dir, `@/*` alias). shadcn/ui initialized with `--base-color neutral` using the classic `shadcn@3.8.5` CLI — the `shadcn@latest` (4.x) CLI has switched to a new preset/registry system (`--base` now picks `radix`/`base` component libraries, not a Tailwind base color) and is not what we want here; pin to 3.x for `init`/`add` until we deliberately opt into the new system.
  - i18n: next-intl using the `/[locale]/` app-dir pattern (`src/app/[locale]/...`), `src/i18n/{routing,navigation,request}.ts`, `src/middleware.ts`, messages in `apps/web/messages/{hi,en}.json`. Locales `hi` (default), `en`. `/` redirects to `/hi`.
  - Fonts: Inter (latin) + Noto Sans Devanagari, both exposed as CSS vars and wired into the Tailwind `--font-sans` stack in globals.css so Hindi and English share one font pipeline.
  - State/data: TanStack Query wired via a client `Providers` component in `src/app/providers.tsx`; Zustand installed, no store yet (first one lands with the first feature that needs client state).
  - packages/shared: `@prayasup/shared`, plain TS source consumed directly (no build step) — `type: module` + `main`/`types` pointing at `src/index.ts`. Both apps resolve it via the pnpm workspace symlink; Next.js needs `transpilePackages: ["@prayasup/shared"]` in next.config.ts for this to bundle correctly, and the package itself needs `"type": "module"` or Node's CJS/ESM interop breaks re-exports (`export *`) when tsx imports it from apps/api.
  - apps/api: Express + TS, run via `tsx watch`. Had to add `--exclude '**/node_modules/**'` to the `tsx watch` script — in this pnpm workspace, tsx's default watch ignore doesn't cover the hoisted root `node_modules`, so without the flag the API kept hot-restarting on unrelated dependency file changes.
  - SSE helper at apps/api/src/lib/sse.ts (`createSseConnection` → `{send, close}`), demo route `/api/v1/stream/ping` streams 5 one-second ticks then closes.
  - Root `pnpm dev` uses `concurrently` to run both apps with prefixed/colored logs.
  - Verified: `pnpm dev` boots web (:3000) + api (:4000); `curl /api/v1/health` → `{"data":{"ok":true},"error":null}`; SSE ticks stream correctly; `/hi` and `/en` both render with correct copy; language switcher works.
- Session 2 (2026-07-05): full database schema.
  - Supabase CLI added as a workspace dev dependency (`pnpm supabase …`); `supabase init` committed (`supabase/config.toml`). Linking (`supabase link --project-ref …`) is a per-machine step; the ref lives in `supabase/.temp/` (gitignored).
  - Complete schema in `supabase/migrations/0001`–`0014`: `pgcrypto` + `vector` (in the `extensions` schema, so vector types/opclasses are schema-qualified as `extensions.vector` / `extensions.vector_cosine_ops`); 17 tables; enums; `set_updated_at()` trigger on every table; pgvector `embeddings` with an HNSW cosine index.
  - Bilingual convention DECIDED — see the Dev conventions section: JSONB `*_i18n` columns, `i18n_complete()` helper, `questions.publish_gate_ok` generated column + publish-gate trigger.
  - RLS ENABLED on all tables with wide-open dev policies in `0013_dev_permissive_rls.sql` (marked "REPLACED IN AUTH PHASE") — strict per-user policies land in Session 15.
  - Dev user seeded with fixed id `00000000-0000-4000-8000-000000000001` (`0014_seed_dev_user.sql`) → set as `DEV_USER_ID` in apps/api/.env.
  - `@supabase/supabase-js` added to apps/api; `pnpm --filter api verify:schema` (apps/api/scripts/verify-schema.ts) HEAD-counts every table via the service-role key. Docs in `supabase/README.md`.
  - Edge-case audit (live-tested): fixed one latent bug — `answer_submissions.question_id` was `ON DELETE SET NULL` which collided with the `has_prompt` CHECK (deleting a referenced question failed with a cryptic 23514); now `ON DELETE RESTRICT` (`0016`). Strengthened the MCQ publish gate (`0017`) via `question_publishable(...)` — MCQ now requires ≥2 bilingual options + a matching `correct_option_key`; descriptive stays stem-only. Accepted-for-now (revisit Session 15): `tests`/`current_affairs` have no publish gate; `*_i18n` shape validated by zod at the API layer; the public anon key has full RW under permissive RLS.
- Session 2.5 (2026-07-05): migrated apps/web from Next.js to a Vite + React SPA. Express API, packages/shared, and the Supabase schema/data are untouched.
  - apps/web rebuilt from scratch: `pnpm create vite@latest`/`create-vite` react-ts template, React 19. `server.port: 3000` in vite.config.ts (unchanged CORS contract with the API) plus a `@/ -> src` alias mirrored in tsconfig.json/tsconfig.app.json and vite.config.ts.
  - Styling: Tailwind v4 via `@tailwindcss/vite`, then `shadcn@3.8.5 init --base-color neutral` (same 3.x-pin rationale as Session 1 — `shadcn@latest`'s `--base` flag now means component-library preset, not Tailwind base color).
  - Routing: react-router v7 (pinned explicitly — `pnpm add react-router` resolves v8 by default now), `createBrowserRouter` in `src/router.tsx`. `/` and unmatched paths redirect (loader-based) to `/${DEFAULT_LOCALE}`; `/:locale` is a lazy layout route (`src/routes/locale-layout.tsx`) that 404-redirects invalid locales and syncs `i18n.changeLanguage` + `document.documentElement.lang`/`dataset.locale` via effect; `/:locale` index is a lazy `src/routes/landing.tsx`. Redirect-only routes need an explicit `Component`/`HydrateFallback` (even if it returns `null`) or react-router logs dev-mode console warnings about the empty leaf route — this trips the "no console errors" DoD if left default.
  - i18n: i18next + react-i18next, resources ported verbatim from the old `messages/{hi,en}.json` into `src/messages/{hi,en}.json`. `src/lib/locale.ts` holds `SUPPORTED_LOCALES`/`isLocale`/`switchLocale` (pure path-rewrite helper); `src/hooks/use-locale.ts` reads the `:locale` URL param as source of truth.
  - DECIDED mid-session: default locale is **`en`**, not `hi` (overrides the Session-1 next-intl default and this task's original brief) — `DEFAULT_LOCALE` in `src/lib/locale.ts` is the single place this lives; everything else (router redirects, i18next `lng`/`fallbackLng`) derives from it.
  - Fonts: `@fontsource-variable/inter` + `@fontsource/noto-sans-devanagari` (400/500/700), both imported as side effects in `main.tsx`; needed ambient `declare module` entries in `src/vite-env.d.ts` since bare-specifier CSS package imports have no shipped types. Single font-family stack (Inter, Noto Sans Devanagari) in `body`; `line-height: 1.75` gated on `:root[data-locale="hi"]`.
  - Data/state: `@tanstack/react-query` provider in `main.tsx` (`retry: 1`, `staleTime: 30_000`); `zustand` installed, no store yet. `@microsoft/fetch-event-source` installed for the future `src/lib/sse.ts` (not built yet — no stream consumer on this landing page).
  - Env: `apps/web/.env.example`/`.env.local` now `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`/`VITE_API_URL`, read only via `import.meta.env`. Repo-wide grep for `NEXT_PUBLIC_`/`next/*` confirmed clean — nothing left to remove.
  - Verified with a headless-browser pass (Playwright, no `chromium-cli` on this machine): `/` client-redirects to `/en`, `/hi` ⇄ `/en` toggle rewrites the URL and swaps copy (Devanagari renders correctly with the taller line-height), API health box shows live `{"data":{"ok":true},"error":null}`, zero console errors/warnings, no horizontal overflow at 390px or 1440px.
- Session 3 (2026-07-05): content ingestion pipeline — CLI scripts in `apps/api/src/ingest`, run via `pnpm ingest:<name>` (root scripts delegate to the `api` workspace). Full docs in `docs/content-pipeline.md`.
  - Shared libs: `src/lib/models.ts` (the ONE model-id constants module — `claude-sonnet-5` reasoning/vision, `claude-haiku-4-5` high-volume; never inlined), `src/lib/anthropic.ts` (client + `structuredJson` via `output_config.format` streaming + `translate`), `src/lib/supabase.ts` (service-role client), `src/lib/embeddings.ts` (swappable `EmbeddingProvider`; OpenAI `text-embedding-3-small`), `src/ingest/_shared.ts` (manifest, PDF extract + scanned detection, paper defs, i18n, reporter).
  - Migration `0018_ingest_support.sql`: `syllabus_nodes.path`+`meta`+unique`(paper_code,path)`; `questions.external_id`+`meta`+unique`external_id`; `tests.slug`+`meta`+unique`slug`; `embeddings.chunk_index`+unique`(source_type,source_id,locale,chunk_index)`. Additive (tables were empty). Pushed via `supabase db push --db-url` (direct IPv6) — see `SUPABASE_DB_PASSWORD` in `apps/api/.env` and [[supabase-headless-migrations]].
  - EMBEDDING DECISION (recorded): OpenAI `text-embedding-3-small`, **1536-dim** (matches `extensions.vector(1536)` + HNSW cosine index from `0012`), behind `EmbeddingProvider` so the vendor is swappable; keep dim=1536 or the column+index must change. One row per (source, locale, chunk).
  - Paper codes are globally unique so `(paper_code, path)` never collides across stages: `PRE_GS1`, `PRE_CSAT`, `MAINS_GH`, `MAINS_ESSAY`, `MAINS_GS1..GS6`.
  - PDF reality (IMPORTANT): the UPPSC PDFs encode Hindi in a **legacy non-Unicode font** — the embedded text layer is mojibake (garbled latin, 0 Devanagari codepoints). `pdf-parse` returns garbage; even `claude-sonnet-5`'s native PDF read transcribes the broken text layer. So the PYQ path (a) extracts with sonnet's native PDF read for clean English + structure, then (b) **detects mojibake Hindi** (`isMojibakeHindi` in `_shared.ts`: expected Devanagari but got latin, differs from English) and **regenerates Hindi from the clean English via batched `claude-haiku-4-5` translation** (`translateBatch`), flagging `meta.machine_translated=true`. Syllabus structuring sidesteps this because sonnet renders correct Devanagari from its own knowledge. Scanned-PDF vision fallback is wired in `ingest:syllabus` but the 2026 syllabus PDFs text-extract cleanly (not triggered).
  - `ingest:pyq` writes `content-raw/parsed/pyq_*.json` (git-ignored review artifacts) and STOPS; `ingest:pyq:load` loads reviewed JSON. `is_published` only when bilingual-complete (publish gate, enforced by DB trigger too).
  - RUN: `ingest:syllabus` end-to-end → **294 syllabus_nodes** across all 10 papers, 100% bilingual from source (0 machine-translated, both official PDFs parsed), tree parent-linkage verified (0 orphans). `ingest:verify` confirms per-paper counts. `ingest:pyq` run on ONE paper (`uppsc_prelims_2024_gs1`) and STOPPED for review: **150/150 questions extracted, 150/150 answer-key-verified** against the official key (cross-check caught 1 discrepancy at q29 → kept official), **150/150 syllabus-classified**, Hindi regenerated for 307 mojibake strings (150 questions machine_translated, flagged), **138/150 bilingual-complete** (12 match-list/statement Qs had incompletely-captured options → stay drafts under the publish gate). tests/embed built + typecheck-clean; not yet run (await PYQ review + `ingest:pyq:load`, then `ingest:tests`/`ingest:embed`).
- Between sessions (2026-07-05): the core Express API layer landed separately (`apps/api/src/routes/{dashboard,profile,syllabus,tests,questions,current-affairs}.ts` + matching `services/`, plus `dev-user.ts`/`rate-limit.ts`/`validation.ts`/`async-handler.ts`/`http-error.ts` and a global error-handling middleware) with matching zod schemas added to `packages/shared` and TanStack Query hooks in `apps/web/src/hooks/`. Committed as "Build the core Express API over the populated schema" + a follow-up edge-case-fix commit, both ahead of this session's work.
- Session 4 (2026-07-05): application shell in `apps/web`, replacing the placeholder landing page.
  - Design system authored as a reusable skill at `.claude/skills/frontend-design/SKILL.md` (read it before touching any UI). Palette: Ink `#12141C` / Chalk `#F5F6FA` / Rajdhani Blue `#2B4EE0` (primary) / Marigold `#F2A93B` / Tulsi `#1E9E6C` / Coral `#E5484D` — deliberately not cream+serif+terracotta, not near-black+acid-green, not a broadsheet look, not stock shadcn neutral gray. Signature element is **the Rubric Dial** (`src/components/ui-x/score-gauge.tsx`): a graduated 180° SVG arc gauge with a coral→marigold→tulsi band, used for score stats and as the Answers (flagship) nav accent — reads like an exam-hall meter, not a fitness ring.
  - Layout: one `NAV_ITEMS` config (`src/lib/nav.ts`) drives both the desktop sidebar (`components/app-shell/sidebar.tsx`, all 7 items) and the mobile bottom tab bar (`bottom-tab-bar.tsx`: 4 primary items + a "More" `Sheet` for the rest = 5 tabs). `routes/app-shell.tsx` is the shared layout route nested under `/:locale`, rendering `TopBar` + `Sidebar`/`BottomTabBar` + `Outlet` + the global `CommandPalette`; page title comes from each leaf route's `handle: { titleKey }` via `useMatches()`.
  - `TopBar` (`components/app-shell/top-bar.tsx`): language toggle rewrites the URL via `switchLocale` AND persists through `PATCH /profile` (`useUpdateProfile`) AND `localStorage` (`LOCALE_STORAGE_KEY` in `lib/locale.ts`); streak flame reads the real `profile.streak_count`; theme toggle backed by a small zustand store (`stores/theme-store.ts`) that toggles the `.dark` class and persists to `localStorage`, initialized from `prefers-color-scheme` on first load.
  - Command palette (`components/ui-x/command-palette.tsx`): built on `cmdk` (named exports `CommandDialog`/`CommandInput`/`CommandList`/`CommandItem`/`CommandGroup`/`CommandEmpty` — NOT `Command.Dialog` sub-property access, both work but named exports typecheck more directly), global `Ctrl/Cmd+K` listener owned by the component itself via a tiny zustand store (`stores/command-palette-store.ts`). Searches real nav items + the live syllabus tree (flattened, capped at 60 rows to keep the modal DOM light) — no mock data.
  - `Sheet` (`components/ui-x/sheet.tsx`) is hand-rolled on the `radix-ui` unified package's `Dialog` namespace (`Dialog.Root/Trigger/Portal/Overlay/Content/Title/Close` — this repo uses the single `radix-ui` meta-package, not per-primitive `@radix-ui/react-*` packages) rather than pulling a generic shadcn sheet, to keep every surface on the same token system.
  - All 7 section routes (`dashboard`, `learn`, `practice`, `answers`, `current-affairs`, `revision`, `profile`) render real Supabase-backed data with no mocks — including genuine empty states where the dev-seed DB has zero rows (e.g. 0 descriptive questions, 0 current-affairs items). Added `use-questions.ts`/`use-current-affairs.ts` hooks + `queryKeys.questions` since only dashboard/profile/syllabus/tests hooks existed before. `Profile` is a real working settings form (display name/target year/medium) wired to the existing `useUpdateProfile` mutation, not just a stat display — the natural feature there is small enough to finish outright, unlike Practice/Answers/Revision which front full subsystems (test-taking, AI evaluation, FSRS) explicitly out of scope for this session.
  - GOTCHA: installing a new frontend dependency (`cmdk`) mid-session and then reusing an already-running `vite` dev process causes `504 (Outdated Optimize Dep)` errors on every dynamic import until `apps/web/node_modules/.vite` is cleared and the dev server restarted — always bounce the dev server after adding a package, don't just rely on Vite's automatic re-optimization.
  - Verified with a headless Playwright pass (no `chromium-cli` on this machine; drove `playwright` directly via `{ chromium }`, `--no-sandbox`): both locales render (Hindi in real Devanagari, English fine), dark mode inverts correctly including the gauge bands, 1440px sidebar layout and 390px bottom-tab-bar + "More" sheet layout both correct, `Ctrl+K` opens the palette and fuzzy-filters nav + syllabus results, visible focus rings confirmed via Tab, zero console errors across all 7 routes in both viewport sizes.
- Session 5 (2026-07-05): built out `/:locale/dashboard` for real, replacing the Session-4 placeholder (stat cards + a plain bar chart) with 6 data-driven cards.
  - New `exam_calendar` table (`0022_exam_calendar_and_daily_quiz.sql`) powers the greeting's "days until next Prelims" countdown. The seed row (`0023_seed_exam_calendar.sql`) — UPPSC Prelims 2026, **2026-12-06** — was verified via web search against multiple independent sources (theiashub, pw.live, adda247, drishtiias, examdetail, cross-checked against each other) citing the official exam calendar released 2026-01-30 and the PCS-2026 notification released 2026-06-25; per CLAUDE.md, NOT seeded from memory. Same migration adds `tests.scheduled_date` so a `daily_quiz`-kind test can be looked up for a specific calendar day (kind alone is ambiguous once more than one exists).
  - `GET /dashboard/summary` (`apps/api/src/services/dashboard.ts`) fully rewritten to return `{greeting, continue, today, performance, weakness_radar, answer_spotlight}` — see `packages/shared/src/dashboard.ts` for the schema (the `continue` field is a discriminated union: `attempt | syllabus_node | none`). No new REST endpoints were needed beyond enriching this one summary — the existing `POST /events` already covered the one new write path.
  - "Continue where you left off" picks whichever is more recent: the latest unfinished `attempts` row (`submitted_at is null`), or the latest `syllabus_node_view` event — a new event name fired from `Learn.tsx` (`use-record-event.ts` → `POST /events`) when a user clicks a syllabus node's title (previously the title text wasn't clickable at all, only the expand chevron was).
  - Weakness radar attributes each graded `attempt_answers` row up to its **top-level syllabus node** (depth 1, i.e. the paper root's direct children like "Indian Polity and Governance") via `syllabus_nodes.path` — `node.path.split('/')[0]` joined with `paper_code` finds the ancestor, since a materialized path has no parent-chain query otherwise. Rendered as a weakest-first bar list (not a literal spider/radar chart — illegible at 390px with 7+ topics), color-banded coral/marigold/tulsi via the same thresholds as the Rubric Dial (`src/lib/score-band.ts`, new shared helper).
  - Added `recharts` (only for the "last 5 scores" sparkline, per the brief) — CSS custom properties work directly in its SVG `stroke`/`fill` props (`var(--chart-4)` etc.), matching the `--chart-1..5` tokens already reserved for this in `index.css`. Deliberately did NOT recolor sparkline points/line by score band — a single trend series keeps one color for its identity; only the per-topic/per-paper bar lists (genuinely distinct entities) use band-coloring.
  - GOTCHA: `stores/theme-store.ts` defaults to **light regardless of OS `prefers-color-scheme`** — only an explicit toggle-click persists dark mode to `localStorage`. (The Session-4 log entry above says it's "initialized from `prefers-color-scheme`," which is no longer accurate — the code was the source of truth here.) A Playwright `colorScheme: 'dark'` browser-context option alone does nothing; you have to actually click the `TopBar` theme button.
  - Verified end-to-end via `curl` against the running API (unfinished attempt → submit → re-check `continue`/`performance`/`weakness_radar` all update correctly) before touching the UI, then a headless Playwright pass: both locales, light/dark, 390px/1440px, zero console errors, and a real click-through of the new Learn-page tracking (`POST /events` → 201). Synthetic test data (one attempt + its answers, one event) was deleted afterward so the dev DB stays at genuine zero-activity baseline; the empty-state screenshots in this session are the *real* brand-new-user state, not a mocked one.
- Session 6 (2026-07-05): built out `/:locale/learn`, `/learn/:paperCode`, `/learn/:paperCode/:nodeId` for real, replacing the Session-4 flat-tree placeholder with a papers grid → expandable outline → node-detail drill-down.
  - Realized the papers grid needs no new "paper" concept or hardcoded title list: every paper already has a real root row in `syllabus_nodes` (`depth=0`, `path=''`, `title_i18n` = the paper's own title, set at ingestion) — `getPaperSummaries` (`apps/api/src/services/syllabus.ts`) just reads those 10 roots and annotates each with real `topics_count`/`pyq_count`/`accuracy_pct` from `syllabus_nodes`/`questions`/`attempt_answers`. New shared schemas in `packages/shared/src/learn.ts` (`PaperSummary`, `SyllabusNodeWithStats`, `SyllabusNodeDetail`).
  - `GET /syllabus/papers/:code/tree` (`getPaperTree`) annotates every node in a paper's tree with **subtree-aggregated** PYQ count + accuracy (own count/correct rolled up through every ancestor via a single deepest-first pass over the flat row set) — a chapter-level row would otherwise always show 0, since only leaf topics carry `questions.syllabus_node_id` directly. `GET /syllabus/nodes/:id` (`getNodeDetail`), by contrast, deliberately reports **own-only** (exact-match) counts, identical to the `/questions?node=` filter it sits next to — the two numbers must never disagree.
  - Extracted `apps/api/src/lib/graded-answers.ts` (`getGradedAnswers`) out of `services/dashboard.ts`'s weakness-radar query so the same "this user's graded MCQ answers, joined to their question's paper/node" data source backs the dashboard, the papers grid, the paper outline, and node detail — one query shape, four call sites.
  - New write paths, both greenfield (enum/table support existed but nothing wrote to them yet): `POST /tests/custom {node_id, count}` (`createCustomTestFromNode` in `services/tests.ts`) builds a `kind:'custom', is_published:true` test from a shuffled sample of the node's own published PYQs — "Practice this topic" on the node-detail page calls this, then shows an inline success banner + link to `/practice` (no attempt-taking UI exists yet, so this is the whole flow for now). `POST /srs/cards/from-node {node_id}` (`apps/api/src/services/srs.ts`, `addNodeToRevision`) creates an `srs_cards` row with `source_type:'manual'` from the node's title/description — idempotent (a second click on the same node returns the existing card instead of duplicating).
  - Outline page's per-node "Practice PYQs" action deep-links to `/practice?node=<id>` — `practice.tsx` now branches on that search param into a `PyqFilterView` instead of the plain test list. Both this filtered view and the node-detail page share `components/learn/pyq-list.tsx` (grouped-by-year, paginated via `?page=`, MCQ options with the correct one highlighted, explanations collapsed behind a per-question toggle — bilingual (EN+HI stacked) when expanded, unlike the rest of the locale-scoped UI, since revision content benefits from both).
  - Migrated the three existing `?node=` deep-links (`continue-card.tsx`, `weakness-card.tsx`, `command-palette.tsx`) to the new `/learn/:paperCode/:nodeId` route — required adding `paper_code` to `dashboardContinueSchema`'s `syllabus_node` variant and to `dashboardWeaknessNodeSchema` (both previously only carried `syllabus_node_id`). Also made `weakness-card.tsx` topic titles clickable for the first time. Command palette's syllabus search now excludes `depth === 0` rows (paper roots aren't real topics to search for — they're reached via the Learn grid instead).
  - GOTCHA (caught by Playwright at 390px, not by typecheck/lint): the outline page's node row initially put the chevron + title + PYQ badge + accuracy chip + two action buttons all in one `flex flex-wrap` line — at 390px this didn't wrap onto multiple lines as expected, it squeezed the `flex-1` title down to a single visible character instead, because `flex-wrap` wraps whole items, not a shrinking `flex-1` item that has room to compress first. Fixed by explicitly stacking title (its own row) above a second `flex-wrap` row for badges/actions, rather than relying on wrap behavior to rearrange one row into two.
  - No DB migration needed — every new endpoint reads/writes tables and enum values (`srs_source_type: 'manual'`, `test_kind: 'custom'`) that already existed from Sessions 2/3, just previously unused.
  - Verified via `curl` against every new endpoint (including the idempotent-add-to-revision double-call and a 400 on `/tests/custom` for a node with zero PYQs), then a headless Playwright pass driving real clicks (expand a node, add-to-revision → "Added", create a custom test → success banner, follow the dashboard's "Continue reading" link end-to-end) across both locales and 390px/1440px. All synthetic `srs_cards`/`tests` rows created during verification were deleted afterward.
- Session 7 (2026-07-05): the MCQ engine, part 1 — `/:locale/practice` (PYQ Papers / Sectional / Custom tabs) and the full test-taking flow at `/:locale/practice/test/:testId` (pre-start → full-screen player → results), all against the real Session-3 PYQ data (132-question PRE_GS1 2024 paper + its sectional splits).
  - Backend additions on top of the attempts/tests services that already existed (start/answers/submit, `POST /tests/custom` — both landed between earlier sessions, not built from scratch here): `GET /attempts/:id` (attempt + its `attempt_answers`, for hydrating a resumed player); `startAttempt` is now idempotent per `(user_id, test_id)` — a second "Start Test" click returns the existing **unsubmitted** attempt instead of creating a duplicate, so the server-authoritative `started_at` (and the countdown derived from it) survives a page reload; `listTests`/`getTestDetail` now join the dev user's submitted attempts to surface `best_score`/`attempts_count` per test; `createCustomTestBody` gained an optional `difficulty` filter.
  - New shared schema: `attemptDetailSchema` (`{attempt, answers}`) for the resume-hydration endpoint. No DB migration needed — everything reads/writes existing `attempts`/`attempt_answers`/`tests` columns.
  - `src/lib/offline-queue.ts`: a generic localStorage-persisted retry queue (`createOfflineQueue`), written provider-agnostic so the later PWA offline-sync work can reuse it directly. Contract: callers always enqueue the FULL current record for a dedupe key (never a partial patch) — the queue does last-write-wins per key with no merge logic of its own, which is what keeps it simple. `useAttemptAnswers` wraps one queue instance per attempt and autosaves every option pick + per-question time-spent tick via `POST /attempts/:id/answers`.
  - Test player (`components/practice/test-player.tsx`) lives at a router path **outside** the `app-shell` layout (`router.tsx`) — deliberately no sidebar/bottom-tab/topbar chrome during an exam, own minimal header instead (title, language toggle, countdown). One question per screen, numbered palette (answered=tulsi/marked=marigold/unanswered=muted, current ring), keyboard shortcuts (arrows navigate, 1-4 pick the option at that position regardless of its A/B/C/D key, `m` toggles mark), `beforeunload` guard while in progress. Mobile: the palette moves into the existing `Sheet` bottom-sheet component instead of the always-visible `lg:`-only sidebar. "Marked for review" is client-only state (not a DB column) persisted to `localStorage` per attempt id.
  - Countdown timer is computed once from `attempt.started_at + test.duration_minutes` (never a client-side elapsed-time clock) and auto-submits on expiry; tests with no `duration_minutes` (sectional/custom) simply show no timer and never auto-submit.
  - Language toggle mid-test is local component state only (`displayLocale`, seeded from the route locale) — since `stem_i18n`/`options_i18n` are already both-locale JSONB, flipping it is instant with zero refetch, confirmed by watching the countdown chip hold its exact value across a toggle in the Playwright pass.
  - Added a hand-rolled `components/ui/tabs.tsx` (radix-ui `Tabs` namespace, same pattern as the existing hand-rolled `Sheet`) since no shadcn primitives beyond `Button` had been added yet in this repo.
  - GOTCHA (real-data check, not from memory): a Playwright `selectOption({label: "UPPSC Prelims — General Studies Paper I"})` call against the Custom-tab paper `<select>` timed out — the paper's stored `title_i18n.en` is `"Prelims — General Studies Paper I"` (no "UPPSC" prefix; that prefix only gets added when `ingest:tests` builds the *test* title). Selecting by `paper_code` value instead of label fixed it — a reminder that real ingested copy won't always match what a session log or a schema field name implies.
  - Verified against the real Session-3 data end-to-end: `curl` through start → resume (same attempt id returned) → answer → submit → re-submit (409) → a fresh post-submit attempt (new id, confirming resume only applies to unsubmitted attempts) → `best_score`/`attempts_count` reflecting the real submitted score; a headless Playwright pass across both locales and 390px/1440px covering all three Practice tabs, the full player (select/mark/navigate/language-toggle/submit-confirm/results), the mobile palette bottom sheet, and the Custom builder creating a real 3-question set end to end — zero console errors throughout. All attempts and custom `tests` rows created during verification were deleted afterward so the dev DB stays at genuine zero-activity baseline.
- Session 8 (2026-07-05): `/:locale/practice/attempt/:attemptId/result` — the post-submit results page, replacing the old inline `TestResults` (deleted; its submit flow now navigates here instead of holding local result state).
  - `GET /attempts/:id/result` (`getAttemptResult` in `services/attempts.ts`) is additive, computed entirely from existing data — no new migration. It recomputes marks/percentile/timing from `attempts.meta` (question_ids/question_marks/marking_scheme) + `attempt_answers` rather than re-deriving `is_correct` (that stays whatever `submitAttempt` persisted), so a retracted-since-submission question can't silently flip a review row's correctness.
  - Percentile is `(scores_below + 0.5*scores_tied) / total_submitted_attempts_for_this_test * 100` across **every** submitted attempt on the test, not just the dev user's — with one real user today that's the same population, but the query itself doesn't filter by `user_id`. Topic breakdown groups review rows by the question's own `syllabus_node_id` (the same leaf a `/learn/:paperCode/:nodeId` deep link needs), with an unlinked "unmapped" bucket for PYQs that never got a node assigned; "weak" = attempted > 0 and accuracy < 50%.
  - New SSE endpoint `GET /stream/explain/:questionId?locale=` (`stream.ts`) generates a missing MCQ explanation with `claude-haiku-4-5`, streaming deltas in the requested locale, then `translateBatch`-style single `translate()` for the other locale, and persists both into `questions.explanation_i18n` — a repeat call (any locale) short-circuits to the stored value instead of re-billing the model. Had to explicitly tell the model "no markdown, no headers, no bold asterisks" in the system prompt; without it haiku free-styled `#`/`**` formatting that rendered as literal characters in the plain-text review UI.
  - `POST /srs/cards/from-question` (`addQuestionToRevision`) mirrors the existing from-node path onto the same `(user_id, source_type, source_id)` unique index, `source_type='question'` — front = stem, back = correct option + explanation (whichever locale each field has).
  - Frontend: `useExplainQuestion` wraps `streamEvents` per-question (one hook instance per review row, not a keyed map — simpler since each row is already its own component); on the `done` event the parent page patches the cached `attemptResult` query directly via `queryClient.setQueryData` instead of refetching. Topic breakdown and review render as stacked flex rows/cards (not a literal `<table>`), consistent with the dashboard's earlier call to avoid layouts that don't survive 390px.
  - "Ask a doubt" is a literal stub per the brief: a `Link` to `/:locale/doubts?question=&attempt=` even though no `doubts` route exists yet — clicking it today falls through to the catch-all redirect; wiring the real route later is a router-only change.
  - Verified against real Session-3 PYQ data end-to-end: built a 5-question custom test from a "Constitution of India" node, submitted a mixed attempt (2 correct/2 incorrect/1 skipped) via `curl`, confirmed `/result`'s accuracy/percentile/avg-time/topic-breakdown math by hand, then a headless Playwright pass (`playwright` installed as a temporary devDependency for the session and removed afterward — not a project dependency) across both locales and 390px/1440px: streamed a real explanation end-to-end (confirmed idempotent re-request returns instantly with no second model call), added a question to revision, zero console errors. All attempts/custom `tests`/`srs_cards` rows created during verification were deleted afterward.
- Session 9 (2026-07-06): the FLAGSHIP — AI Answer-Writing Evaluation engine in the API. New service dir `apps/api/src/services/evaluation/` (`rubric.ts`, `grounding.ts`, `prompts.ts`, `evaluate.ts`), shared contract in `packages/shared/src/evaluation.ts`, `POST /answers/submissions` (`routes/answers.ts`), and the SSE runner `GET /stream/evaluations/:submissionId` (added to `stream.ts`).
  - Rubric **v1** (`rubric.ts`) — six weighted dimensions summing to exactly 1.0 (a startup assertion enforces it): structure_flow .20, content_coverage .30, keywords_concepts .15, examples_data .15, presentation .10, word_limit_language .10. Each scored 0-10; weighted fraction projected onto the question's max marks. `RUBRIC_VERSION`/dimension keys live in shared; weights + examiner descriptions are server-only.
  - Two-pass evaluation with `claude-sonnet-5`: **pass 1** = strict structured-JSON analysis (per-dimension score+justification, reference_points, missed_key_points, factual_errors, is_off_topic, overall_comment) via `structuredJson`; **pass 2** = three streamed calls — strengths, then improvements, then a within-word-limit model answer. SSE order (spec-exact, live-verified): `status → dimension_score ×6 → analysis → feedback_delta (strengths, improvements) → model_answer_delta → done {evaluation_id}`. Persists everything to `evaluations` incl. aggregated tokens + cost (Anthropic model spend; the RAG embedding cost is sub-cent-negligible and each call is also logged to `llm_calls`).
  - **RAG grounding** (`grounding.ts` + migration `0027_match_embeddings.sql`): a `match_embeddings` RPC does cosine-distance ANN over the pgvector `embeddings` store (PostgREST can't express a `<=>` ORDER BY). The operator/type are addressed as `OPERATOR(extensions.<=>)` / `extensions.vector(1536)` because pgvector lives in the non-search_path `extensions` schema. Grounding embeds the question text (OpenAI, via `lib/embeddings.ts`), pulls node-scoped chunks first then top-k global, dedupes by id, and **degrades gracefully** (empty grounding, evaluate ungrounded) on any embed/RPC error. Real: 852 live embeddings in the dev DB.
  - Guardrails: max input length (zod — `typed_text` ≤20k, custom prompt ≤2k, empty rejected), **one concurrent evaluation per user**, cost logged via `llm_calls`, and an honesty guardrail (off-topic/empty/injection → low scores + `is_off_topic=true`, never hallucinated praise). Migration `0028` adds `answer_submissions.meta` to carry a custom prompt's word_limit/marks from the POST to the later SSE GET.
  - `MODEL DECISION` (recorded): `claude-sonnet-5` has **no `temperature`** knob (deprecated → 400). Determinism for scoring comes from `output_config.effort`, not temperature. Pass-1 scoring runs at `effort: "low"` — the rubric's explicit score bands do the calibration, and low effort minimizes the divergent exploration that makes a strong answer's borderline dimensions (8-vs-9) wobble run-to-run. Extended `lib/anthropic.ts` with an additive `onUsage` callback (token/cost aggregation) and an optional `signal` (AbortSignal) so a disconnected SSE client cancels the in-flight stream instead of burning tokens.
  - Eval harness `pnpm eval:answers [--runs N] [--keep] [--lang hi]` (`apps/api/scripts/eval-harness.ts`): runs a strong / mediocre / off-topic sample answer (editable fixtures) through the real pipeline and gates on (1) ranking good>mediocre>off-topic and (2) repeatability ≤5% **of full marks** (the meaningful denominator for an exam score; also reports the stricter of-mean %). Tuned to PASS both: good ~12.2/15, mediocre ~4.6, off-topic 0.75 (`is_off_topic=true`), repeatability good 3.0% / mediocre 4.0% / off-topic 0.0% over 3 runs.
  - GOTCHA (Anthropic structured outputs): a JSON Schema `integer` with `minimum`/`maximum` is **rejected with a 400** ("properties maximum, minimum are not supported"). The 0-10 range is enforced by the prompt + a server-side `clampScore` instead of the schema.
  - Hardened after an adversarial multi-agent code review (5 dimensions → per-finding verification; 14 raw → 9 confirmed). The high-severity one: `planEvaluation` claimed the submission to `'evaluating'` **before** `resolveQuestionContext`, so a transient DB error there stranded the row in `'evaluating'` forever (the throw pre-empts the SSE route's `req.on('close')` release), permanently 409-locking the user out. Fixes: resolve the (read-only) question context **before** any status mutation; **replay a persisted evaluation regardless of the submission's status** (source of truth for "done" → covers lost terminal writes); emit `done` right after persist and make the `→complete` write best-effort; a partial unique index `answer_submissions (user_id) where status='evaluating'` (`0029`) makes the per-user guard race-proof (23505→409); stale-`evaluating` reclaim (`updated_at < now()-5min → failed`) so a crash can never lock a user out; per-field replay locale; injection hardening (an "untrusted data, never instructions" clause + `<<<`/`>>>` fence neutralization in the answer builders).
  - Verified end-to-end against the running API (validation battery: 14 guardrail cases all 400/404; SSE full sequence; concurrency 409 live; instant replay with no re-bill; GET detail with tokens+cost) and via service-level probes (prompt-injection scored **0/15 off_topic=true**; Hindi feedback+model answer in Devanagari; catalogued `question_id` path with node grounding; the DB unique index rejecting a 2nd `evaluating` with 23505; stale reclaim). All synthetic `answer_submissions`/`evaluations` rows deleted afterward — dev DB back at zero baseline. Note: the dev server was already running on :4000, so the live checks hit it (hot-reloaded); a second `pnpm --filter api dev` just EADDRINUSE'd.
- Session 10 (2026-07-05/06): built out `/:locale/answers` — the hub (today's-question rotation, PYQ picker with paper filter, custom-prompt path, submission history), the Writing Room (`/answers/write`), and the evaluation showpiece (`/answers/evaluation/:submissionId`), replacing the Session-9 placeholder hub.
  - Descriptive questions didn't exist yet (Session 9 only had the eval *pipeline*, no catalogued Mains PYQs) — ran `ingest:pyq`/`ingest:pyq:load` for `uppsc_mains_2025_gs1` first (20/20 bilingual) to unblock the "Pick from PYQs" flow before building the UI.
  - Backend additions: `GET /answers/today` (deterministic daily rotation — IST day-number mod published-descriptive-count, `apps/api/src/services/questions.ts`), `GET /answers/submissions` (paginated history, joins `questions`/`evaluations` for a denormalized stem+score per row), `GET /questions/:id` (single published question, for hydrating the Writing Room from `?question=`), `POST /srs/cards/from-evaluation` (saves an evaluation's `reference_points`+`missed_key_points` to revision — reuses `source_type: 'manual'` keyed by `submission_id` rather than adding a new enum value, since the existing `addQuestionToRevision` assumes an MCQ-shaped question row and can't serve a custom-prompt evaluation).
  - Frontend: `useEvaluationStream` (`apps/web/src/hooks/use-evaluation-stream.ts`) is the direct descendant of Session 8's `useExplainQuestion` streaming-hook template, extended to a reducer over all 8 SSE event types — the same accumulator code path handles both a live run (many small `feedback_delta`/`model_answer_delta` chunks) and a replay (each arrives as one full-text event), since both just append to a string. `ScoreGauge` (the Rubric Dial) is reused as-is for both the six per-dimension dials (`score/10 * 100`) and the overall-score hero (`overall_score/max_score * 100`), per the frontend-design skill's own note that it's meant for "future evaluation results" — no new gauge component needed. Added `framer-motion` (new dependency) for the dimension dials' staggered reveal-in, respecting `useReducedMotion`. Writing Room: draft autosave to localStorage (`use-draft-autosave.ts`, ref-based single interval so continuous typing doesn't perpetually defer the save), UPPSC time-pressure countdown presets (125w/7min, 200w/11min — self-paced start/pause, not server-authoritative like the MCQ test-player's timer), and Web Speech API dictation (`use-dictation.ts`, hi-IN/en-IN toggle, feature-detected — renders nothing on browsers without `SpeechRecognition`).
  - GOTCHA (caught by Playwright at 390px in Hindi, not by typecheck): `SectionCard`'s header row (`flex items-center justify-between`, no wrap) breaks when a long Hindi title shares the row with a `<select>` action — the title box shrinks and wraps to 4+ lines instead of the row wrapping. Fixed by moving the PYQ-picker's paper-filter `<select>` out of the `action` slot and into the card body as its own row above the list, rather than trying to out-shrink the select.
  - Verified end-to-end against the running API with a REAL (non-synthetic-in-the-sense-of-fake, but cleaned up after) submission: full SSE run (grounding→analysis→6 dimension_scores→feedback→model_answer→done, ~4.76/8 real sonnet score), instant idempotent replay (0.25s, no re-bill), history list join, `POST /srs/cards/from-evaluation` idempotent add. A deliberately-truncated first `curl` (piped through `head -c`) confirmed the disconnect-release safety path: the submission correctly flipped to `'failed'` instead of hanging. Then a full Playwright pass (both locales, 390px/1440px, light+dark) — zero console errors. All synthetic submission/evaluation/srs-card rows deleted afterward.
  - Follow-up same session: user flagged that only 2 of 10 papers had any PYQs (Prelims GS1 2024, Mains GS1 2025) — the Session-3 ingestion was a single-paper proof of concept, not a full run. Content pipeline already had PDFs downloaded (via `content:fetch`, gitignored binaries) for 5 more Prelims papers (2023/2024/2025 × CSAT/GS1) and 15 more Mains papers (2024/2025 × GS1-6/Essay/General Hindi) — asked the user for ingestion scope (small/Mains-only/full), got **full breadth**. Ran all `ingest:pyq` parse steps **in parallel** (per explicit user request) as 19 background Bash tasks, then `ingest:pyq:load --all`: **999 questions upserted, 486 published**.
  - DATA REALITY (recorded): **prelims MCQ questions can only publish with a verified `correct_option_key`**, and the content-raw manifest only has an official UPPSC answer key for **2024** Prelims (CSAT + GS1) — 2023 and 2025 Prelims papers extract fine but load as `is_published=false` drafts (0/100, 0/145, 0/100, 0/150 bilingual-complete) since `questionPublishable()` requires a correct answer matching an option, and none exists to cross-check against. This is the ingest pipeline correctly declining to publish an MCQ with an unverified/unknown correct answer, not a bug — Prelims PYQ coverage today is genuinely only 2024 (CSAT 100 + GS1 132 published). Mains (descriptive) has no such gate (only bilingual stem completeness), so all 8 Mains papers × both years published in full (`MAINS_GS2`-`GS6` = 40 PYQs each, `MAINS_ESSAY`/`GH` = 18/16). Missed `uppsc_mains_2024_gs1` in the first parallel batch (still-manual paper-list bookkeeping error, not a pipeline issue) — caught it because `MAINS_GS1` showed 20 instead of 40 like its siblings, ingested it separately to close the gap.
  - NOTE: mid-session, a second concurrent process (untracked `apps/api/src/services/ocr/`, a new migration `0030_answer_images_bucket.sql`, and edits to `packages/shared/src/evaluation.ts`/`apps/api/src/routes/{answers,stream}.ts`/`apps/api/src/services/evaluation/{evaluate,prompts}.ts`) was actively building handwritten/OCR answer-submission support — out of scope for this session (per Session 9's "typed-only" note) and left untouched/uncommitted; only fixed the couple of type errors it transiently introduced in code this session owns (`listSubmissions`' `SubmissionListRow` needed a `mode` field once `submissionSchema` gained OCR columns).
