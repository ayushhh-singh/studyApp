# Server
PORT=4000

# Supabase (cloud project — create at supabase.com, same project used for local + prod)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# LLM
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Dev-only auth stand-in (removed in Session 15)
DEV_USER_ID=
