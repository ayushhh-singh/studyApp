# Explicit self-rewrites for every top-level static file in public/ that
# must NEVER fall through to the SPA shell — placed ABOVE the wildcard so
# there is no reliance on Cloudflare's implicit "exact static file wins over
# _redirects" priority (that assumption held under local `wrangler pages dev`
# but real-world reports showed a direct browser navigation to /sitemap.xml
# rendering the app shell instead of the file). _redirects rules are
# evaluated top-to-bottom, first match wins, so each of these claims its path
# before the catch-all ever sees it.
/sitemap.xml               /sitemap.xml               200
/robots.txt                /robots.txt                200
/favicon.svg               /favicon.svg               200
/manifest.webmanifest      /manifest.webmanifest      200
/manifest.hi.webmanifest   /manifest.hi.webmanifest   200
/og-default-en.png         /og-default-en.png         200
/og-default-hi.png         /og-default-hi.png         200
/offline.html              /offline.html              200
/assets/*                  /assets/:splat             200
/pwa/*                     /pwa/:splat                200
/marketing/*               /marketing/:splat          200

# Cloudflare Pages SPA fallback — mirrors apps/web/vercel.json's rewrite rule.
# Vite copies public/* verbatim into dist/, so this ships as dist/_redirects.
# Cloudflare Pages serves an exact-match static file (e.g. the prerendered
# dist/en/index.html) BEFORE evaluating this rule, so /en and /hi still hit
# their prerendered snapshots, not this fallback — verified with
# `wrangler pages dev dist` (see docs/operations.md's Cloudflare Pages section).
#
# DELIBERATELY still the plain "/* -> /index.html 200" form, NOT
# "__spa-fallback.html" — a same-day attempt at that "fix" (see git history,
# reverted) turned into a REAL, LIVE infinite redirect loop in production:
# Cloudflare Pages auto-strips the .html extension from ANY served path
# (redirecting /__spa-fallback.html -> /__spa-fallback), which immediately
# loops back into this exact rule forever (confirmed live: curl showed 50+
# chained 308s all pointing at /__spa-fallback, and /en/dashboard — every
# real authenticated app route — was completely down). Reverted within
# minutes of shipping. Cloudflare's build-time linter DOES still flag this
# literal line as a false-positive "infinite loop" and silently drops it
# (acknowledged bug in Cloudflare's own tooling:
# https://github.com/cloudflare/workers-sdk/issues/11824) — but Cloudflare
# Pages has its own separate, undocumented built-in "serve index.html for an
# unmatched SPA path" default that transparently covers for the dropped rule,
# verified live repeatedly. That undocumented-but-working default is safer
# than any alternative explicit rule tried so far — do not "fix" this again
# without shipping to a real preview/staging deploy first, not straight to
# production, given how fast this exact class of change broke live traffic.
/*   /index.html   200
