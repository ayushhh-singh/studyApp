{
  "name": "prayasup",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "echo \"dev script placeholder — will run web + api concurrently once scaffolded (Session 1)\" && exit 1"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.0.0"
}
